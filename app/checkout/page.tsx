import { CheckoutClient } from "@/components/checkout-client"

export const metadata = {
  title: "Checkout — Nula CRM",
  robots: { index: false, follow: false },
}

export default function CheckoutPage() {
  return <CheckoutClient />
}
